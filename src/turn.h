#include "stdio.h"
#include "baseDesk.h"

desk *turn_fig_choice(desk *board);
desk *turn_pose_pick(desk *board);
void turn_of_fig(desk *selected_figure, desk *selected_pose, desk *board, int *p_turn, int *P_turn);